# --- AWS Configuration ---
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here

# --- Database (RDS) - Shared Database ---
DB_HOST=your-rds-endpoint.rds.amazonaws.com
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_db_password_here

# --- Storage (S3 Buckets) ---
STT_BUCKET_NAME=stt-service-storage-dev
CHAT_BUCKET_NAME=chat-service-storage-dev
TTS_BUCKET_NAME=tts-service-storage-dev
DOCUMENT_BUCKET_NAME=document-reader-storage-dev
QUIZ_BUCKET_NAME=quiz-service-storage-dev

# --- Kafka (EC2 Private IPs) ---
KAFKA_BOOTSTRAP_SERVERS=10.0.0.1:9092,10.0.0.2:9092,10.0.0.3:9092

# --- Application Environment ---
APP_ENV=production

# --- AI Services ---
GEMINI_API_KEY=gen-lang-client-0048645107

# --- Authentication ---
# Generate a secure random string for JWT token signing
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your_secure_jwt_secret_here_change_this_in_production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24
